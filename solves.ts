function solve(requiredLetters: string, optionalLetters: string): string[] {
  /** array like `['a']` */
  const requiredCharacters = [...requiredLetters];
  /** array like `['b', 'c', 'd']` */
  const optionalCharacters = [...optionalLetters];

  return [];
}

console.log("solution:", solve("a", "bcdefg"));
